<div class="post">
  <h2 class="title">All Pages</h2>
  <div class="entry">
    <ul>
      <% @pages.each do |page| %>
        <li><%= link_to page.page_date, page_path(page.page_date) %>
      <% end %>
    </ul>
  </div>
</div>